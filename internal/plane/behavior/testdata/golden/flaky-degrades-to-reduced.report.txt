grip: BLOCKED (exit 2)
planes: behavior · governed modules: 2 · ungoverned: 0

fail-closed (1):
  ✖ cannot verify module src/checkout's boundary placeOrder because its captured output is nondeterministic (confidence reduced) — remove run-to-run noise (time, ordering, random ids) so its snapshot is stable, then pin it.
