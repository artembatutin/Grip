// An internal helper of the domain module. It is NOT part of the module's
// entrypoint (index.ts) surface, so reaching it from another module is an
// internal-reach violation.
export class OrderInternals {}
